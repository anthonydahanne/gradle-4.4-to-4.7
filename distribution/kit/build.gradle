apply plugin: 'distribution'

def commonDistSpec = {

  into('client/ehcache/javadoc') {
    from project(':terracotta-ehcache-client').javadocJar
  }

}


distributions {
  main {
    contents ({} << commonDistSpec)
  }
}
